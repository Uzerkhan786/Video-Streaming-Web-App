import React from 'react'

const Buttons = ({ name }) => {
    return (
        <div >
            <button className='bg-gray-300 p-2 ' type='button'>{name}</button>

        </div>
    )
}

export default Buttons
